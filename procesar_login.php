<?php

// condicional que verifica que se hayan rellenado los campos usuario y contraseña del formulario de login
if (isset($_POST["usuario"]) && isset($_POST("contrasena"))) {

    //consulta a base de datos donde el usuario y la contraseña introducidos tienen que concluir con un unico registro de la base de datos

    // condicional que maneja que se encuentre al usuario
    if () {
        //redireccionar a la pagina principal
    } else {
        //redireccionar a login con mensaje de error
    }

}